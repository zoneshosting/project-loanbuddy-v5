import { Typography } from '@mui/material';
import MainCard from 'ui-component/cards/MainCard';

const Notes = () => (
  <MainCard title="Lead Notes">
    <Typography variant="body2">
      Track interactions and add notes for each lead. Document important details, conversations, and next steps in the mortgage application process.
    </Typography>
  </MainCard>
);

export default Notes;