import { Typography } from '@mui/material';
import MainCard from 'ui-component/cards/MainCard';

const Payments = () => (
  <MainCard title="Loan Payments">
    <Typography variant="body2">
      Track and manage loan payments. View payment history, upcoming payments, and payment schedules.
    </Typography>
  </MainCard>
);

export default Payments;