import dashboard from './items/dashboard';
import leads from './items/leads';
import utilities from './items/utilities';

const navigation = {
  items: [dashboard, leads, utilities]
};

export default navigation;